# ir_agent Reference Extract

This directory contains the implementation modules directly referenced by the thesis appendix. It is a reference extract, not the default public runtime. The reproducible public path is `tools/check_entry.py` and `demos/run_recorded_demo.py`.
