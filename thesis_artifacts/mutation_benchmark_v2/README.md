# Mutation Benchmark v2

This public bundle includes aggregate reports under `reports/`. Individual `cases/<case_id>/` workspaces are represented by the neutral mapping in `docs/thesis_artifact_mapping.md` because they may contain source fragments from the private methodology corpus.

Neutral case layout used in the thesis: `source_original.md`, `source.md`, `normalized.md`, `original_ir_reference.a4v3`, `manual_ir_working.a4v3`, `backend_surface_ir.a4v3`, `<case_id>.json`, `notes.md`, `manual_ir_working_metrics_v1.json`, and `backend_results.json`.
